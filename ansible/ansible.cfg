[defaults]
inventory         = inventory.ini
host_key_checking = False

# The targets are rebuilt often and their host keys change every time.
# On anything long-lived you would leave host key checking on.
